fastapi==0.115.0
uvicorn[standard]==0.30.0
sqlalchemy==2.0.35
python-multipart==0.0.12
httpx==0.27.0
tenacity==8.2.3
deepgram-sdk==3.7.0
pydantic>=2.10,<3
pydantic-settings>=2.5,<3
sse-starlette==2.1.0
pytest==8.3.0
pytest-asyncio==0.24.0

PyPDF2==3.0.1
python-docx==1.1.2

# Database migration & Postgres support
alembic==1.14.0
asyncpg==0.30.0
psycopg2-binary==2.9.10

# Supabase client & JWT verification (HITL feedback system)
supabase>=2.5
pyjwt[crypto]>=2.8

# pgvector semantic search on agent_learnings (Phase J Task 29)
pgvector>=0.3
openai>=1.0

# Durability — Inngest workflow engine (Phase D.1)
inngest>=0.5,<1.0

# Tracker XLSX import + export (Phase v3-tracker)
openpyxl>=3.1,<4

# Optional: dotenv for diagnostic scripts
python-dotenv>=1.0
pytest-cov==5.0.0

# Wave 2 observability — metrics, structured logs, error tracking
prometheus-fastapi-instrumentator==7.0.2
python-json-logger==2.0.7
sentry-sdk[fastapi]==2.18.0

# Wave 3 durability — S3/MinIO storage portability
boto3==1.34.144
moto[s3]==5.0.18

# Deal-linker matcher (intake.matcher) — fuzzy name + phonetic + legal-suffix
rapidfuzz>=3.6
jellyfish>=1.0
cleanco>=2.3
