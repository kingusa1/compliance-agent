---
created: 2026-05-10
updated: 2026-05-10
tags: [session, log]
session_date: 2026-05-10
---

# Session log — 2026-05-10

> Long autonomous session. User priorities (in chronological order):
> 1. Verify the test call I uploaded — what's right, what's wrong
> 2. Fix Vercel 404 root cause (kept recurring)
> 3. Make AI 1000000000x smarter
> 4. Build a Quality Agent / multi-agent system
> 5. Save everything to an Obsidian Brain (this vault)

## Major fixes shipped (chronological commits)

### Backend — pipeline accuracy
- `tracker_aggregator.py` — `awaiting_review` 500-crash fixed (positional-arg bug in `_rejection_row`)
- `pipeline.py` — supplier matching via `canonicalize_supplier()` enum compare (was ILIKE; failed because seed had `"EON"` but LLM returned `"E.ON Next"`)
- `pipeline.py` — preserves canonical `"E.ON Next"` label after script match (no more downgrade to seed's `"EON"`)
- `pipeline.py` — V1 fallback when matched script has empty `checkpoints` (no more 0/0 verdicts)
- `pipeline.py` — populates `call.duration_seconds` from Deepgram metadata
- `pipeline.py` — auto-rename stub deal + customer + slug from detected business name (the "Crosby Garage" demo)
- `pipeline.py` — MPAN/MPRN/deal-value entity propagation from extracted entities → `CustomerDeal`
- `pipeline.py` — sibling-supplier inheritance (same-deal + cross-deal via human-name match)
- `pipeline.py` — bidirectional human-name match (`Bank` ↔ `Banks`)
- `pipeline.py` — relaxed stub-merge gate so retries can stitch (only the rename branch is gated on stub label)
- `analysis.py` — V1 prompt now requires per-checkpoint `notes`
- `analysis.py` — hardened `detect_names` prompt + agent==customer collision guard
- `analysis.py` — `LLM_RETRY` skips permanent 4xx (saves ~3min on 402/401/403/404)
- `transcription.py` — signal-based agent vs customer speaker labelling
- `schemas.py` — `RuleCheckpoint` gains optional `notes`

### Backend — Quality Agent
- New file: `backend/app/quality_agent.py` — Opus 4.7 cross-call identity resolver
  - System prompt: senior compliance analyst persona, vocabulary-validated JSON
  - `find_sibling_candidates(call_id, db)` — bucket by name overlap
  - `resolve_identity(calls)` — the actual Opus call (LLM_RETRY-wrapped)
  - `apply_verdict_to_db(bucket, verdict, db)` — applies merge
  - `auto_resolve_for_call(call_id, db)` — pipeline one-shot
- `routes.py` — `POST /api/admin/quality-resolve` — runs across all completed calls
- `pipeline.py:_step_finalize` — auto-runs Quality Agent on every upload

### Frontend — UX
- New: `frontend-v3/src/components/design/HelpBanner.tsx` — dismissible inline help
- New: `frontend-v3/src/components/design/PipelineTimeline.tsx` — 5-stage "What the AI did" panel on call detail
- New: `frontend-v3/src/app/not-found.tsx` — branded 404 with quick-links
- HelpBanner mounted on: Dashboard, Tracker, Queue, Rejections, Customers, Deals
- N-stage workflow label + tooltip on customer detail's WorkflowBar (E.ON 2-stage, others 3-stage)
- Phase labels humanised (`lead_gen` → "Lead Gen" etc.)
- `CheckpointCard.tsx` — AI Verdict falls back to evidence when notes missing
- `calls/[id]/page.tsx` — duration resolves from real value, no more hardcoded 4:18 fallback
- `CallsList.tsx` — trash icon delete on hover with confirm step + cache invalidation across calls/customers/deals/dashboard

### Deploy hygiene
- New top-level `Dockerfile` — mirrors `backend/Dockerfile` with `backend/` prefix on COPY paths so Railway GitHub auto-deploys can build
- New top-level `railway.json` — `watchPatterns = ["backend/**", "Dockerfile", "railway.json"]`
- `backend/railway.toml` — removed `watchPatterns` (was breaking `railway up` CLI snapshots)
- **Vercel `rootDirectory` patched via API** from `null` → `frontend-v3` (THE 404 root cause)
- Vercel `framework` patched from `null` → `nextjs`
- `vercel.json` — `github.enabled: false`, `git.deploymentEnabled.main: false`

### Live verification
- All 17 frontend routes return 200/307 on `compliance-agent-mu.vercel.app`
- Branded `/not-found` serves "ComplianceAI" page (not raw Vercel 404)
- Backend `/api/health` and `/healthz` 200
- Test calls verified end-to-end (see [[05_State/Test_Calls]])

## The flagship moment
**Quality Agent merged 3 fragmented Church customers into 1.**

Input:
- `St. Peter's Benfleet Church` — 1 call, no deal
- `Evangelical Church` — 1 call, no deal
- `The Church` — 1 call, no deal

Output (via `POST /api/admin/quality-resolve`):
```json
{
  "resolved": 1,
  "changes": [{
    "bucket_size": 3,
    "canonical_name": "Dorothy's Evangelical Church",
    "supplier": "E.ON Next",
    "confidence": 0.92,
    "stitch_reason": "All three calls reference Christopher Neil Banks, Evangelical Church, same postcode, E.ON Next contract with agent Afak."
  }]
}
```

After:
- Customer `dorothy's evangelical church` — 3 calls, 1 deal, `suppliers: ['E.ON Next']`

The Quality Agent also figured out that **`Afak` is the agent** (we'd been mis-tagging him as the customer).

## Context items the user clarified
- Tomorrow (2026-05-11) is a **PROJECT HANDOVER**, not a live demo
- The user wants multi-agent orchestration — "agents talk to each other"
- The user wants this Brain so future Claude sessions can resume seamlessly
- The user wants Playwright MCP installed for live page-by-page verification (added; awaits restart)

## Commits made this session
Approximate count: ~20 commits to `main` branch, kingusa1/compliance-agent.

See [[05_State/Live_State]] for the current verified state.
See [[07_Tomorrow/Project_Handover]] for the demo script.
